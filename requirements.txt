Django
djangorestframework
drf-spectacular
drf-spectacular-sidecar

# JWT / auth
PyJWT

# Database (pick the right one)
psycopg2-binary   # if PostgreSQL

# Emails (since you have send_verification_email)
sendgrid # if using SendGrid

# Utilities
python-dotenv # for .env handling (optional)
django-cors-headers  # if exposing APIs cross-origin